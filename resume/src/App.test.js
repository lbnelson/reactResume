import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

test('todo', () => {
  // Low-pri todo since the real test is a nicely formatted PDF getting spit out.
});
